function renderShoppingListPage() {
    const wrapper = document.getElementById('wrapper');

    renderNavContainer(wrapper);
    renderShoppingListContainer(wrapper);
    renderFooterContainer(wrapper);
}

renderShoppingListPage();